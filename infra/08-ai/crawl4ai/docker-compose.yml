x-tier-label: &tier-label
  hy-home.tier: ai

# Crawl4AI fetches arbitrary URLs, so it is an SSRF-capable service. It runs on
# its own egress network and never joins another repository network: internal databases, secret
# stores and admin APIs are not reachable from the crawler. A consumer that
# needs it (for example Open Notebook's CRAWL4AI_API_URL) must join
# crawl4ai_net explicitly in the same reviewed change.
networks:
  crawl4ai_net:
    name: crawl4ai_net
    driver: bridge

services:
  crawl4ai:
    profiles:
      - crawl4ai
    extends:
      file: ../../common-optimizations.yml
      service: template-infra-high
    image: unclecode/crawl4ai:0.9.3
    container_name: crawl4ai
    user: appuser
    # Chromium needs more memory and processes than the high template allows.
    mem_limit: 4g
    pids_limit: 512
    shm_size: 1gb
    read_only: true
    tmpfs:
      - /tmp
      - /var/lib/redis:uid=999,gid=999,mode=0700
      - /var/lib/crawl4ai/outputs:uid=999,gid=999,mode=0700
      - /home/appuser/.crawl4ai:uid=999,gid=999,mode=0700
      - /home/appuser/.cache/url_seeder:uid=999,gid=999,mode=0700
      - /home/appuser/.gunicorn:uid=999,gid=999,mode=0700
    secrets:
      - crawl4ai_api_token
    # Since 0.9.0 the server binds beyond loopback only with a token, and then
    # requires `Authorization: Bearer` on every endpoint except GET /health.
    command:
      - /bin/bash
      - -ec
      - |
        CRAWL4AI_API_TOKEN="$$(tr -d '\r\n' </run/secrets/crawl4ai_api_token)"
        [ "$${#CRAWL4AI_API_TOKEN}" -ge 16 ] || { echo 'crawl4ai: crawl4ai_api_token must be at least 16 characters' >&2; exit 64; }
        export CRAWL4AI_API_TOKEN
        exec bash entrypoint.sh
    expose:
      - '11235'
    healthcheck:
      test: ['CMD', 'curl', '-fsS', 'http://127.0.0.1:11235/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - crawl4ai_net
    labels:
      <<: *tier-label
      traefik.enable: 'false'

<!-- Target: docs/07.guides/01-gateway/01.setup.md -->

# 01-gateway Setup Guide

> Step-by-step procedure for deploying and configuring the entry point infrastructure.

---

## Overview (KR)

이 문서는 `01-gateway` 티어의 초기 설정 및 배포 가이드이다. 도메인 연결, 인증서 준비, 그리고 컨테이너 실행을 아키텍처 모범 사례에 따라 수행할 수 있도록 돕는다.

## Guide Type

`system-guide`

## Target Audience

- Infrastructure Operator
- Backend Developer
- Contributor

## Purpose

This guide helps the reader deploy a fully functional edge router (Traefik) and path proxy (Nginx) with TLS termination and service discovery.

## Prerequisites

- Docker & Docker Compose installed.
- Valid domain name (configured in `DEFAULT_URL` environment variable).
- Secrets generated via `scripts/gen-secrets.sh`.
- Certificates available in `secrets/certs/`.

## Step-by-step Instructions

### 1. Initialize Network

Ensure the shared infrastructure network exists:

```bash
docker network create infra_net
```

### 2. Configure Traefik

1. Review `infra/01-gateway/traefik/config/traefik.yml`.
2. Ensure dynamic configuration in `infra/01-gateway/traefik/dynamic/` is present.
3. Verify TLS certificates are mapped correctly in `tls.yaml`.

### 3. Deploy Gateway Stack

Start the core gateway components:

```bash
cd infra/01-gateway
# Start Traefik (Primary Edge)
docker compose up -d traefik

# Start Nginx (Path Proxy) if needed
docker compose up -d nginx
```

### 4. Verify Functionality

- Access the Traefik dashboard (internal only).
- Test `/ping` endpoint on Nginx/Traefik.
- Verify TLS handshake via browser or `curl -vI`.

## Common Pitfalls

- **Cert Name Mismatch**: Ensure `tls.yaml` points to the correct filenames in `secrets/certs/`.
- **Port Conflicts**: Port 80 and 443 must be available on the host.
- **Network Isolation**: Backend services must be on the `infra_net` to be discovered by Traefik.

## Related Documents

- **Spec**: [../04.specs/README.md](../../04.specs/README.md)
- **Operation**: [../08.operations/01-gateway/README.md](../../08.operations/01-gateway/README.md)
- **Runbook**: [../09.runbooks/01-gateway/README.md](../../09.runbooks/01-gateway/README.md)

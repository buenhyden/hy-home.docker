# 📂 Repository Structure

이 프로젝트의 디렉토리 구조와 각 폴더가 담당하는 역할에 대해 상세히 설명합니다.

## 📦 Root Level

| 디렉토리/파일 | 설명 |
| :--- | :--- |
| **`infra/`** | 프로젝트의 핵심이 되는 Docker Compose 기반 인프라 정의 파일들이 위치합니다. |
| **`projects/`** | 인프라 위에서 구동될 실제 애플리케이션 프로젝트나 실험적인 코드들이 위치하는 작업 공간입니다. |
| **`docs/`** | 프로젝트의 모든 문서가 저장되는 곳입니다. (아키텍처, 가이드, 표준 등) |
| **`scripts/`** | 데이터 백업, 복구, 초기화, 배포 등을 자동화하기 위한 쉘 스크립트 모음입니다. |
| **`.agent/`** | AI 에이전트(Claude, ChatGPT 등)가 프로젝트를 이해하고 작업을 수행하기 위한 지침과 페르소나 설정입니다. |
| **`secrets/`** | **[중요]** `.gitignore`에 등록되어 git에 커밋되지 않는 민감한 정보(비밀번호, 키 파일 등)를 저장하는 로컬 전용 폴더입니다. |
| **`certs/`** | SSL/TLS 인증서 파일이 위치하는 곳입니다. |
| **`archive/`** | 더 이상 사용되지 않거나 참고용으로 보관된 구버전 설정 및 파일입니다. |
| **`.github/`** | GitHub Issue Template, PR Template, Actions Workflow 등 GitHub 플랫폼 관련 설정입니다. |

## 🏗 `infra/` Directory

인프라 디렉토리는 서비스 도메인별로 하위 폴더가 나뉘어져 있습니다.

- **`observability/`**: 모니터링 스택 (Prometheus, Grafana, Loki, Tempo 등)
- **`postgresql-cluster/`**, **`redis-cluster/`**: 고가용성 데이터베이스 클러스터 설정
- **`traefik/`**, **`nginx/`**: 리버스 프록시 및 게이트웨이 설정
- **`keycloak/`**, **`vault/`**: 보안 및 인증 서비스
- **`minio/`**: 오브젝트 스토리지
- **`kafka/`**, **`n8n/`**, **`airflow/`**: 메시징 및 워크플로우 자동화

각 하위 폴더는 해당 서비스를 구동하기 위한 `docker-compose.yml` (또는 `.override.yml`)과 설정 파일(`conf/`)을 포함합니다.

## 📝 `docs/` Directory

문서 디렉토리는 역할에 따라 세분화되어 있습니다.

- **`adr/`**: 아키텍처 결정 기록 (Architecture Decision Records)
- **`guides/`**: 사용자 가이드 및 튜토리얼
- **`standards/`**: 개발 및 문서 작성 표준
- **`ops/`**: 운영 관련 매뉴얼
- **`setup/`**: 설치 및 환경 구성 가이드

## 🧪 `projects/` Directory

개발자가 실제 코드를 작성하고 테스트하는 샌드박스입니다. 이 폴더 내의 각 프로젝트는 `infra/`에서 띄운 서비스들(DB, AI API 등)을 활용하여 개발을 진행합니다.

- 각 프로젝트 별로 독립적인 `.env` 설정을 가질 수 있습니다.
- 인프라에 의존성을 가지지만, 인프라 설정 자체를 변경하지는 않는 것을 원칙으로 합니다.

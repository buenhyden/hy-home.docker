---
title: "MongoDB Operations Policy"
version: "1.0.2"
type: "operation/policy"
status: "active"
owner: "@buenhyden"
updated: "2026-09-20"
layer: "operations"
artifact_id: "POL-0027"
parent_ids:
- "AD-0004"
created: "2026-05-17"
---

# MongoDB Operations Policy

## Overview

이 정책은 `hy-home.docker`의 `LAB` MongoDB replica set 운영 기준을 정의한다. runtime image/version은 Compose declaration이 소유하며, 정책 기준은 exact `mongodb` profile, `MyReplicaSet`, `mongo-key` named volume, Docker Secret 기반 root/UI credential이다.

## Policy Scope

- `infra/04-data/nosql/mongodb/docker-compose.yml`
- `mongo-key-generator`, `mongodb-rep1`, `mongodb-rep2`, `mongodb-arbiter`, `mongo-init`, `mongo-express`, `mongodb-exporter`
- `mongo-key`, `mongodb1-data`, `mongodb2-data`, `mongodb3-data`
- `mongodb_root_password`, `mongo_express_basicauth_password`, `MONGODB_ROOT_USERNAME`, `MONGO_EXPRESS_CONFIG_BASICAUTH_USERNAME`
- Traefik route `mongo-express.${DEFAULT_URL}` and exporter port `${MONGO_EXPORTER_PORT:-9216}`
- Linked guide and runbook under `docs/05.operations`

## Controls

- **Required**: Documentation must describe the current replica set as two data-bearing nodes plus one arbiter, initialized by `mongo-init`.
- **Required**: Replica set name references must use the compose-declared `MyReplicaSet`; no undocumented replica-set-name control is allowed.
- **Required**: Root and Mongo Express password guidance must reference Docker Secret mounts, not plaintext values.
- **Required**: Keyfile guidance must reference the `mongo-key` named volume generated by `mongo-key-generator`.
- **Required**: All seven services use the exact `mongodb` profile; the same-host topology must not be described as host-level HA.
- **Required**: Backup uses authenticated `mongodump --oplog` against a data-bearing replica-set member, includes all required databases plus users/roles, indexes and oplog metadata, and records tool/server compatibility, checksum, retention and restore evidence.
- **Required**: Restore rehearsal uses a fresh isolated compatible replica set and `mongorestore --oplogReplay`; passwords are supplied through protected prompt/file handling and never embedded in a URI or evidence.
- **Required**: Upgrade and removal require a restore-tested backup, capacity check, compatibility review, and explicit approval; the arbiter is never treated as a data copy.
- **Allowed**: Read-only `rs.status()`, `rs.conf()`, logs, compose config, and exporter readiness checks for evidence capture.
- **Allowed**: Documentation-only corrections that keep service names, profiles, and links aligned with compose; runtime pins remain Compose-owned.
- **Disallowed**: Destructive resync, data directory deletion, forced election, keyfile rotation, or backup restore steps without separate approval and verified runbook evidence.
- **Disallowed**: Treating `mongodb-arbiter` as a backup/data node or exposing MongoDB directly through host ports not declared by compose.

## Exceptions

N/A - no currently approved exceptions.

## Verification

- Compare this policy with [MongoDB guide](guide.md), [MongoDB runbook](runbook.md), and [infra README](../../../../../infra/04-data/nosql/mongodb/README.md) after compose changes.
- Run `docker compose --profile mongodb config --quiet` before approving service-name, replica set, route, secret, keyfile, or exporter documentation updates.
- Run `python3 scripts/validation/check-document-links.py --mode all` after policy or linked operations document updates.

## Review Cadence

- Review on MongoDB compose image/profile/secret/keyfile/replica-member changes.
- Review during the Stage 05 operations documentation audit cadence.

## Traceability

- Declared parent: [Data Tier (04-data) Architecture Description](../../../../02.architecture/descriptions/0004-data-architecture.md) (`AD-0004`)
- Subject peers: [Guide](guide.md) (`GDE-0027`), [Runbook](runbook.md) (`RUN-0027`)

## Related Documents

- [Compose implementation: infra/04-data/nosql/mongodb/docker-compose.yml](../../../../../infra/04-data/nosql/mongodb/docker-compose.yml)

- [MongoDB backup and restore tools](https://www.mongodb.com/docs/v8.0/tutorial/backup-and-restore-tools/)
- [MongoDB security hardening](https://www.mongodb.com/docs/v8.0/core/security-hardening/)
- [MongoDB Community licensing](https://www.mongodb.com/legal/licensing/community-edition)

- [Operations index](../../../README.md)
- [Usage guide](guide.md)
- [Recovery runbook](runbook.md)
- [Infra README](../../../../../infra/04-data/nosql/mongodb/README.md)

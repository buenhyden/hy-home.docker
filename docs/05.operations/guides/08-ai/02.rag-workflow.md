---
status: migrated
---
<!-- Target: docs/05.operations/08-ai/02.rag-workflow.md -->

# 02.Rag Workflow Operations

> Migrated from `docs/05.operations/08-ai/02.rag-workflow.md` during the 2026-05-10 operations taxonomy consolidation.

## Usage

### RAG Workflow Usage (08-ai)

> Retrieval-Augmented Generation with Open WebUI & Qdrant (08-ai)

#### Overview

이 가이드는 로컬 문서를 벡터화하여 저장하고, LLM이 관련 정보를 참고하여 답변하도록 하는 RAG 시스템의 구성 방법을 설명한다.

#### Core Components

- **Source**: PDF, Markdown, 텍스트 문서 등.
- **Embedding Model**: 텍스트를 벡터로 변환 (default: `qwen3-embedding`).
- **Vector DB**: 변환된 벡터와 메타데이터를 저장 및 검색 (`Qdrant`).
- **Orchestrator**: 사용자의 질문에 맞춰 관련 문서를 검색하고 프롬프트에 주입 (`Open WebUI`).

#### Implementation Flow

##### 1. 벡터 데이터베이스 연동

- Open WebUI 설정에서 `VECTOR_DB_URL`이 `http://qdrant:6333`을 가리키도록 설정되어 있는지 확인한다.

##### 2. 문서 임베딩 및 업로드

- Open WebUI 인터페이스를 통해 문서를 업로드하면 내부적으로 Ollama의 임베딩 엔진을 호출한다.
- 임베딩된 데이터는 Qdrant의 지정된 컬렉션에 저장된다.

##### 3. 검색 및 응답 (Retrieval)

- 사용자 질문 발생 시 Open WebUI는 Qdrant에서 유사도가 높은 Top-K 문서를 검색한다.
- 검색된 컨텍스트를 시스템 프롬프트에 포함하여 LLM에 전달한다.

#### Optimization Strategies

- **Chunking**: 문서 분할 크기를 조정하여 검색 정확도와 컨텍스트 길이를 최적화한다.
- **Hybrid Search**: 단순 벡터 검색 외에 키워드 기반 검색을 병행하여 정확도를 높인다.

#### Troubleshooting

- **No Context Retrieved**: Qdrant 컬렉션이 비어 있거나 필터 조건이 너무 엄격하지 않은지 확인한다.
- **Embedding Timeout**: 대량의 문서 업로드 시 Ollama 임베딩 엔진의 처리 지연 여부를 확인한다.

#### Related Documents

- [infra/08-ai/open-webui/README.md](../../../../infra/08-ai/open-webui/README.md)
- [04-data/qdrant/](../../../../infra/04-data/specialized/qdrant/README.md)

---

#### Overview (KR)

이 문서는 `docs/05.operations/08-ai/02.rag-workflow.md` 주제의 사용 가이드다. 기존 본문을 기준으로 작업자가 필요한 배경, 절차, 주의사항을 빠르게 찾도록 보강한다.

#### Usage Type

`system-guide`

#### Target Audience

- Developer
- Operator
- AI Agent

#### Purpose

관련 인프라 서비스나 문서 영역을 이해하고 안전하게 변경 또는 운영할 수 있도록 돕는다.

#### Prerequisites

- Repository root README 확인
- 관련 `infra/` 서비스 README 확인
- 필요한 경우 대응 operation/runbook 문서 확인

#### Step-by-step Instructions

1. 관련 README와 기존 본문을 먼저 읽는다.
2. 실제 compose/config 경로와 문서 설명이 일치하는지 확인한다.
3. 변경이 필요하면 대응 템플릿과 상위 README 링크를 함께 갱신한다.
4. 관련 검증 스크립트 또는 문서 audit를 실행한다.

#### Common Pitfalls

- guide 문서에 운영 정책이나 incident timeline을 섞지 않는다.
- secret 값, token, 인증서 원문을 열람하거나 문서화하지 않는다.
- runtime 변경이 필요한 경우 문서 보강과 별도 작업으로 분리한다.

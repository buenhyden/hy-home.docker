---
status: active
---
<!-- Target: docs/05.operations/guides/07-workflow/02.n8n-automation.md -->

# 02.N8N Automation Operations

## Usage

### Overview

이 가이드는 02.N8N Automation Operations의 사용 맥락, 전제 조건, 일반 점검, runbook handoff 기준을 설명한다.

### Usage Type

`how-to | onboarding`

### Target Audience

- Operators
- Developers
- Contributors
- AI Agents

### Purpose

- 02.N8N Automation Operations의 운영 사용 맥락을 빠르게 파악한다.
- 반복 실행 절차와 장애 대응은 연결된 runbook으로 넘긴다.
- 통제 기준은 연결된 policy 문서와 분리해 유지한다.

### Prerequisites

- Repository checkout 접근 가능
- 관련 `docs/03.specs/` 또는 operations 문서 확인 가능
- 필요한 경우 Docker/Docker Compose 명령 실행 권한

### Step-by-step Instructions

1. 이 문서의 overview와 usage context를 확인한다.
2. 관련 service, configuration, 또는 documentation target을 식별한다.
3. `## Common Checks`의 검증 항목을 실행하거나 검토한다.
4. 반복 절차, 장애 대응, rollback, escalation이 필요하면 `## Runbook Handoff`의 runbook으로 이동한다.

### Common Pitfalls

- guide에 policy control이나 복구 절차를 직접 섞어 목적 프로파일을 흐리는 경우
- target-relative link를 템플릿 위치 기준으로 계산하는 경우
- 검증 명령 실행 결과 없이 운영 가능 상태를 단정하는 경우

### n8n Automation Usage (07-workflow)

> Low-code Workflow Automation and Integration Usage (07-workflow)

#### Overview

이 가이드는 n8n을 사용하여 외부 API와 시스템을 통합하고 시각적인 방식으로 자동화 워크플로우를 구성하는 방법을 설명한다.

#### Workflow Principles

- **Visual Logic**: 노드 기반 인터페이스를 사용하여 데이터 흐름을 설계.
- **Webhook Integration**: 외부 이벤트(GitHub, Discord 등)를 실시간으로 수신하여 처리 가능.
- **Secret Management**: n8n 내부의 `Credentials` 시스템을 사용하여 안전하게 인증 정보 관리.

#### Key Procedures

##### 1. 워크플로우 생성 및 실행

- **Node Selection**: 필요한 트리거(Trigger)와 액션(Action) 노드를 선택.
- **Data Transformation**: `Code` 노드(JavaScript)를 사용하여 복잡한 데이터 변환 수행 가능.
- **Activation**: 설계를 마친 워크플로우를 `Active` 상태로 전환하여 자동 실행 활성화.

##### 2. 에러 핸들링

- **Error Workflow**: 워크플로우 실패 시 실행될 전용 에러 핸들링 노드 구성 권장.
- **Notification**: 실패 발생 시 Slack이나 이메일로 즉시 알림 전송.

#### Security Best Practices

- n8n 관리자 계정은 반드시 비밀번호를 사용하여 보호한다.
- 외부 노출이 필요한 Webhook의 경우 `01-gateway` (Traefik) 수준에서 화이트리스트 IP 기반 접근 제어를 검토한다.

#### Troubleshooting

- **Workflow Interrupted**: 실행 중 서버 리소스 부족으로 중단된 경우 로그 확인 필요.
- **API Rate Limit**: 외부 API 호출 시 속도 제한(Rate Limit) 정책을 준수하는지 확인.

## Common Checks

- `HYHOME_COMPOSE_PROFILES='workflow dev' bash scripts/validation/validate-docker-compose.sh`
- Runtime이 실행 중이면 `docker compose exec n8n wget -qO- http://localhost:${N8N_PORT:-5678}/healthz`

## Runbook Handoff

반복 실행 절차, 장애 대응, rollback 또는 escalation 기준은 [n8n recovery runbook](../../runbooks/07-workflow/n8n.md)을 따른다.

## Related Documents

- [Operations index](../../README.md)
- [n8n system guide](./n8n.md)
- [n8n operations policy](../../policies/07-workflow/n8n.md)
- [n8n recovery runbook](../../runbooks/07-workflow/n8n.md)

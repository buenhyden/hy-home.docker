---
status: migrated
---
<!-- Target: docs/05.operations/guides/07-workflow/01.airflow-dag-dev.md -->

# 01.Airflow Dag Dev Operations

> Migrated from `docs/05.operations/07-workflow/01.airflow-dag-dev.md` during the 2026-05-10 operations taxonomy consolidation.

## Usage

### Airflow DAG Development Usage (07-workflow)

> Programmatic Workflows & CeleryExecutor Operations (07-workflow)

#### Overview

이 가이드는 `hy-home.docker` 플랫폼에서 Apache Airflow를 사용하여 데이터 파이프라인(DAG)을 개발하고 운영하는 방법을 설명한다.

#### Architecture

- **Scheduler**: DAG 파일을 읽고 작업 실행 시점을 결정함.
- **CeleryExecutor**: 수많은 워커(Worker) 노드에 작업을 분산하여 처리함.
- **Valkey Broker**: 스케줄러와 워커 사이의 메시지 큐 역할을 수행함.
- **PostgreSQL**: 태스크 상태 및 메타데이터 저장소.

#### Step-by-Step Instructions

##### 1. DAG 작성 표준

- 모든 DAG 파일은 `infra/07-workflow/airflow/dags/` 디렉터리에 위치해야 한다.
- **Idempotency**: 모든 작업은 여러 번 실행해도 결과가 동일해야 한다.
- **Connections**: 외부 DB나 API 접근 정보는 `Admin > Connections`에서 관리한다.

```python
from airflow import DAG
from airflow.operators.python import PythonOperator
from datetime import datetime

with DAG('my_standard_dag', start_date=datetime(2026, 1, 1), schedule_interval='@daily') as dag:
    ### Task definition
    pass
```

##### 2. 모니터링 및 디버깅

- **Web UI**: DAG의 성공/실패 여부를 시각적으로 확인.
- **Flower**: Celery 워커의 부하와 큐 상태를 모니터링 (`https://flower.${DEFAULT_URL}`).
- **Logs**: 각 태스크의 로그는 UI에서 직접 확인하거나 영구 볼륨을 통해 접근 가능.

#### Maintenance Procedures

- **Worker Scaling**: 부하가 높을 경우 `worker` 서비스의 복제본(replicas)을 늘린다.
- **DB Cleanup**: 오래된 작업 기록을 주기적으로 아카이빙하여 메타데이터 DB의 성능을 유지한다.

#### Troubleshooting

- **Task Stuck in Queued**: Valkey 브로커와의 연결 상태를 점검하거나 워커가 실행 중인지 확인한다.
- **DagBag Processing Timeout**: DAG 파일이 너무 무겁거나 외부 리소스를 직접 호출하는지 점검한다.

#### Related Documents

- [infra/07-workflow/airflow/README.md](../../../../infra/07-workflow/airflow/README.md)
- [09. Procedures](./README.md)

---

#### Overview (KR)

이 문서는 `docs/05.operations/07-workflow/01.airflow-dag-dev.md` 주제의 사용 가이드다. 기존 본문을 기준으로 작업자가 필요한 배경, 절차, 주의사항을 빠르게 찾도록 보강한다.

#### Usage Type

`system-guide`

#### Target Audience

- Developer
- Operator
- AI Agent

#### Purpose

관련 인프라 서비스나 문서 영역을 이해하고 안전하게 변경 또는 운영할 수 있도록 돕는다.

#### Prerequisites

- Repository root README 확인
- 관련 `infra/` 서비스 README 확인
- 필요한 경우 대응 operation/runbook 문서 확인

#### Step-by-step Instructions

1. 관련 README와 기존 본문을 먼저 읽는다.
2. 실제 compose/config 경로와 문서 설명이 일치하는지 확인한다.
3. 변경이 필요하면 대응 템플릿과 상위 README 링크를 함께 갱신한다.
4. 관련 검증 스크립트 또는 문서 audit를 실행한다.

#### Common Pitfalls

- guide 문서에 운영 정책이나 incident timeline을 섞지 않는다.
- secret 값, token, 인증서 원문을 열람하거나 문서화하지 않는다.
- runtime 변경이 필요한 경우 문서 보강과 별도 작업으로 분리한다.

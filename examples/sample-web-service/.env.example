# Copy to .env and adjust. Never commit a real .env or any secret value.
# This sample has no secrets; real services reference secrets via env_file,
# Docker secrets, or Vault — never as plaintext in docker-compose.yml.

WEB_HOST_PORT=8080

name:

networks:
  infra_net:
    name: infra_net
    driver: bridge
  infra_net:
    external: true

include:
  - 

services:
  sample:
    image: 
    container_name: 
    hostname: 
    restart: unless-stopped
    ports:
      - 
    expose:
      - 
    networks:
      - 
      -        
    labels:
    volumes:
    environment:
      
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2" 

volumes: